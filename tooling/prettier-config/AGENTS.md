# `@mvp-realty/prettier-config` agent instructions

Root rules in `/AGENTS.md` and tooling rules in `/tooling/AGENTS.md` apply. This package owns shared Prettier formatting.

## Hard rules

- Change formatting conventions here rather than per workspace.
- Keep Tailwind class sorting configured through the shared Prettier setup.
- Run `pnpm format:check` after formatting-related changes.
